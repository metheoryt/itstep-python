# Актуальная информация о каждом модуле доступна по ссылке
# https://docs.python.org/3/library/{имя модуля}.html

# Обработка строк
import string, re

# Работа с аргументами командной строки
import argparse, getopt

# Математические операции
import math, cmath

# Время и дата
import datetime, time, calendar

# Коллекции
import collections, array

# Форматы данных, кодировки
import encodings, base64, pickle, json, csv, gzip, zipfile, zlib, bz2

# Файлы, каталоги, процессы
import sys, os, os.path, glob, io, tempfile, threading

# Сеть, интернет
import cgi, urllib, smtplib, poplib, telnetlib, socket, asyncore
# серверы
import socketserver, http, xmlrpc, asynchat

# XML
import xml  # defusedxml@pypi как безопасная альтернатива


# Другие
import copy  # копирование объектов
import random  # псевдослучайные генераторы
